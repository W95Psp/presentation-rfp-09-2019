section.fv
  div.color-prop-background(n='black', x=0, y=0, b=0, r=0)
  
  div(style='background: white;', n="white", x=0, y=0, b=0, r=0)
  h1(x=1, y=0, n='fstar') F*
  div(x=1, y=2, n='fstar-enum').fstarEnum
    div(at-1='in fadeIn') Type refinement
    div(at-2='in fadeIn') OCaml syntax
    div(at-3='in fadeIn') Proof assistant
    div(at-4='in fadeIn') Effects
  pre.language-fsharp(x=7.5, y=1, at-2='in fadeIn', at-2.1='out').
    let rec add (n m: ℕ)
      : ℕ
      = match n with
      | 0 -> m
      | 0 -> 1 + add (n - 1) m
  pre.language-fsharp(x=7.5, y=1, at-2.1='in', at-4='out').
    let rec add (n m: ℕ)
      : (r: ℕ{r == n + m})
      = match n with
      | 0 -> m
      | 0 -> 1 + add (n - 1) m
  pre.language-fsharp(x=7.5, y=1, at-4='in', n="fstar-code").
    let rec add (n m: ℕ)
      : Tot (r: ℕ{r == n + m})
      = match n with
      | 0 -> m
      | 0 -> 1 + add (n - 1) m
    
    let rec fact (n: ℤ): Div ℕ
      = if n=0 then 1
        else n + fact (n - 1)
  div(x=10, y=-0.5, r=1.4, b=3.7, at-1.25='in slideInDown faster', at-2='out').color-prop-background 
    div(b=0.2, x=0, r=0, style='font-size: 50%; text-align: center;', at-1.25='in fadeIn') TYPES IN USUAL LANGUAGES
  div(x=6, y=0)
    .c.ll(x=1, y=0, at-1.3='in fadeIn', at-2='out')
        span  30
        span  20
        span  10
        span  0
        span -10
        span -20
        span -30
    .c(x=1, y=3.4, at-1.3='in fadeIn', at-2='out') \(r:\mathbb Z \left\{r \textit{~divisible by~} 10\right\}\) <br> smaller
    .c.ll(x=4.75, y=0, at-1.1='in fadeIn', at-2='out')
      span  3
      span  2
      span  1
      span  0
      span -1
      span -2
      span -3
    .c(x=4.75, y=3.4, at-1.1='in fadeIn', at-2='out') \(\mathbb Z\)
    .c.ll(x=7.2, y=0, at-1.2='in fadeIn', at-2='out')
      span <i> 1</i>, <i>2</i>
      span <i> 1</i>, <i>1</i>
      span <i> 0</i>, <i>1</i>
      span <i> 0</i>, <i>0</i>
      span <i>-1</i>, <i>0</i>
      span <i>-1</i>, <i>-1</i>
      span <i>-2</i>, <i>-1</i>
    .c(x=7.2, y=3.4, at-1.2='in fadeIn', at-2='out', style='width: 300px;')  \((\mathbb Z \times \mathbb Z)\)  <br> bigger
  h1(x=1, y=4, n='verasco') Verasco
